# 2026-07-20 工作记录

## 古风水墨 GitHub 个人简介重设计（README.md v4.0）

- 目标：将 cubeyu 的 GitHub 个人简介改为「古风水墨卡片 + 左右交替布局 + 水墨晕染 + 古典字体」，且符合 GitHub 渲染规则。
- 关键约束（GitHub 合规）：会剥离 `<style>`/`<script>`/内联`<svg>`/`class`/`id`；装饰必须走「SVG → <img> 引用」。本项目采用**相对路径**引用仓库内 `svg/` 资源（提交后 github.com 直接渲染），保持 README 简洁。
- 水墨晕染实现：SVG `feGaussianBlur` 滤镜 + `radialGradient` 模拟墨色洇染；远/中/近三层山峦叠加透明度做空间。滤镜在浏览器（GitHub 渲染）中生效。
- 左右交替卡片：HTML `<table>` 双列，`border-collapse:separate; border-spacing` 做间距；卡片用内联样式（纸色 `#f7f1e3`、金线 `#c9b88a`、圆角、柔影 `box-shadow`）。逐段交换图文列顺序实现交替。
- 古典字体栈：`STKaiti,KaiTi,楷体,STFangsong,FangSong,仿宋,serif`（依观者系统回退）。
- 配色：宣纸 `#f7f1e3`、墨 `#1a1a1a`/`#2c2c2c`、松烟褐 `#5a4632`、朱砂 `#a83232`/`#8b1a1a`、金线 `#c9b88a`。
- 新增资源（svg/）：ink-landscape / ink-bamboo / ink-pavilion / ink-goose / ink-brush（插画），section-about/skills/projects/stats/contact（章节标题，带朱砂印 壹–伍）。
- 验证：全部 SVG 通过 XML 校验；README 中 15 个 svg 引用均存在。本地 ImageMagick 在 Git Bash 下路径解析损坏，无法出 PNG 预览，但浏览器渲染无碍。
- 注：`generate_readme.ps1` 在仓库中不存在（project_layout 快照可能过期），最终只改写了 README.md 本身。
